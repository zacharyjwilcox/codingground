#include <stdio.h>

int main()
{
    /*listenForInput();
        buildCourseList(case1):
        showCourseList(case2);
        removeFromList(case3);
        changeCourseCredits(case4);
        calculateGPA(case5);
        exit();
        */
        
        

    

    return 0;
}

